import 'package:flutter/material.dart';

/// Legacy placeholder: 3D Landmark Model is now flexibly managed inside ARStorytellingPanel Column layout
class AR3DViewerOverlay extends StatelessWidget {
  const AR3DViewerOverlay({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
